#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "forth_interpreter"

source = if (filename = ARGV[0])
           Source.new(File.open(filename), alt_print: true)
         else
           Source.new($stdin)
         end
ForthInterpreter.new(source).interpret
